# Cropwise

AI-powered crop recommendation system using weather and soil data (Python + ML + Streamlit).

## Quickstart
1. Create and activate venv: python -m venv .venv && source .venv/Scripts/activate
2. Install deps: pip install -r requirements.txt
3. Run demo: streamlit run app.py
